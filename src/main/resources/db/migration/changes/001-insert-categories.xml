<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
  xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

  <changeSet id="001-insert-categories" author="onenull">
    <comment>Insert default transfer categories</comment>

    <sql> INSERT INTO categories (name, created_at, updated_at) VALUES ('SAVINGS', NOW(), NOW()) ON
      CONFLICT (name) DO NOTHING; </sql>

    <sql> INSERT INTO categories (name, created_at, updated_at) VALUES ('SALARY', NOW(), NOW()) ON
      CONFLICT (name) DO NOTHING; </sql>

    <sql> INSERT INTO categories (name, created_at, updated_at) VALUES ('INVESTMENTS', NOW(), NOW())
      ON CONFLICT (name) DO NOTHING; </sql>

    <sql> INSERT INTO categories (name, created_at, updated_at) VALUES ('VACATION', NOW(), NOW()) ON
      CONFLICT (name) DO NOTHING; </sql>

    <sql> INSERT INTO categories (name, created_at, updated_at) VALUES ('OTHER', NOW(), NOW()) ON
      CONFLICT (name) DO NOTHING; </sql>

    <rollback>
      <sql> DELETE FROM categories WHERE name IN ('SAVINGS', 'SALARY', 'INVESTMENTS', 'VACATION',
      'OTHER'); </sql>
    </rollback>

  </changeSet>

</databaseChangeLog>